<Project Sdk="Microsoft.NET.Sdk">

    <Import Project="..\..\..\props\netcore.props" />
    <Import Project="..\..\..\props\common.props" />
    <Import Project="..\..\..\props\version.props" />
    <Import Project="..\..\..\props\nuget.props" />

    <PropertyGroup>
        <Title>XiHan.BasicApp.AI</Title>
        <AssemblyName>XiHan.BasicApp.AI</AssemblyName>
        <PackageId>XiHan.BasicApp.AI</PackageId>
        <Description>曦寒基础应用 AI 库</Description>
        <OutputType>Library</OutputType>
        <!-- Qdrant 向量库连接器目前仅 preview、无 GA，本库为已知的有意预发布依赖，抑制打包警告 NU5104 -->
        <!-- XHFH001 的标准模板固定为原作者；本次新增文件按实际贡献者标注作者。 -->
        <NoWarn>$(NoWarn);NU5104;XHFH001</NoWarn>
    </PropertyGroup>

    <ItemGroup>
      <ProjectReference Include="..\XiHan.BasicApp.Saas\XiHan.BasicApp.Saas.csproj" />
    </ItemGroup>

    <ItemGroup>
      <!-- Qdrant 向量库连接器（RAG）；实现 Microsoft.Extensions.VectorData 抽象，当前仅 preview，钉版本确保可复现 -->
      <PackageReference Include="Microsoft.SemanticKernel.Connectors.Qdrant" Version="1.74.0-preview" />
    </ItemGroup>

</Project>
